# Goalloom · 开发 TODO

v0.3.1 · 2026-09-21。`[x]`只代表真实文档工作或用户确认，**所有应用功能和应用测试均未完成**。

## 已完成的文档与需求决定

- [x] 根据两张参考图整理五列与目标关联，撰写PRD、架构、验收和Markdown TODO。
- [x] 用户确认Goalloom，告知购买goalloom.com。
- [x] 用户确认首版本地桌面、三个月可自定义、条目只保留一个主位置。
- [x] 沿用“无进度同步”的解释，移除首版聚合百分比和关联状态传播。
- [x] 保留上一版QuickGUI / Tauri / Electron资料评估，不把资料评估当实测。
- [x] 用户确认Electron为首版框架，正式计划不再并行选择Tauri / QuickGUI。
- [x] 用户批准按列历史、往期未完成、分尺度顺延策略和关键变化记录。
- [x] 更新v0.3本地文档；新增历史 / 顺延详细规格并同步相关章节。
- [x] 私人GitHub仓库已创建，初始v0.3提交06e70d5已读回并核对文件树。
- [x] 用户指定Hugeicons作为应用UI图标库；补充图标规范，未安装应用依赖。
- [x] 修正往期入口显示条件、事件链校验边界和过时仓库状态；新增对应验收规格。

## M0 — 剩余实施边界

- [ ] 确认关系是单父树还是允许多个上级的有向无环图；不要用主位置唯一替代答案。
- [ ] 确认三个月先只设置起点、固定三日历月的参数边界，保持历史周期不可静默重写。
- [ ] 确认最低OS / CPU，不默认承诺Intel Mac或Windows ARM64。
- [ ] 确认默认界面语言和首批私人内测范围。
- [ ] 在上述边界确定后冻结首版迁移与支持范围，不重新打开Electron / 本地版选型。

验收：D05、D10、D12–D16保持Accepted；D06等未回答事项保留待定，不伪造批准。

## M1 — Electron最小双平台验证

- [ ] 建立React + TypeScript strict + Vite + shadcn/ui + Tailwind CSS兼容工程并锁版本。
- [ ] 安装并锁定@hugeicons/react与@hugeicons/core-free-icons，使用免费Stroke Rounded，按ICONOGRAPHY.md建立共享图标入口。
- [ ] 审核shadcn新增组件的内置图标，不混用其他UI图标库；保留ref / 焦点 / 可访问性行为。
- [ ] 验证图标显式导入、样式一致、图标按钮名称、深色模式和打包后断网显示；通过H01–H04。
- [ ] 建立Electron main / preload / renderer与共享DTO；只加载本地资源。
- [ ] 暴露有限window.goalloom API，验证来源 / 参数和返回类型，不开放原始IPC / SQL / shell。
- [ ] 配置contextIsolation、renderer sandbox和nodeIntegration=false，测试拒绝非法调用。
- [ ] 选择SQLite驱动，核对Electron内嵌Node能力或原生模块ABI / rebuild / 打包路径。
- [ ] 做最小两列拖拽、中文IME、关联选择器、空列落点、Esc取消和键盘替代。
- [ ] 在macOS和Windows分别打包、安装、运行并验证SQLite保存 / 重启恢复。
- [ ] 验证导入 / 导出文件对话框、中文路径与生产资源加载。
- [ ] 记录运行时 / 驱动 / CPU / OS、冷启动、稳定内存与安装体积；不借用外部benchmark。
- [ ] 选定构建 / 打包工具和测试runner，提交实际工作流；不保留第二套正式桌面后端。
- [ ] 建立可注入时钟的日历 / 历史测试入口；生产不暴露测试控制端口。

验收：真实Electron双平台闭环可运行；浏览器截图或宿主Node数据库测试不算通过。

## M2 — 五列、持久化与早期关键事件

- [ ] 建立settings、policies、items、periods、placements、operations、events和迁移表。
- [ ] 实现日历服务：真实月 / 周 / 当地日、自定义三个月原始锚点与固定历史UTC边界。
- [ ] 创建条目 / 主位置 / 创建事件 / 命令结果同事务，保证稳定ID与单一主位置。
- [ ] 实现录入、编辑、完成、重开、取消、归档、软删除与恢复。
- [ ] 从第一版写入就记录关键计划 / 状态 / 可见性事件，不等历史UI完成后补造。
- [ ] 实现操作ID去重、请求摘要、实体version与失败回滚；响应丢失重试不重复事件。
- [ ] 实现独立due_date，移动或顺延不会自动改变它。
- [ ] 实现列内排序、跨列拖动、空列落点、边缘滚动与“移动到…”。
- [ ] 区分同尺度跨期顺延、跨尺度移动和同周期排序；排序不制造延期事件。
- [ ] 实现本列折叠完成区、全局已完成按实际completed_at分组、详情和中文子串搜索。
- [ ] 实现窄窗口导航、可见焦点、键盘操作、主题和窗口尺寸恢复。
- [ ] 通过A01–A10、D01–D03和事件原子性基础用例。

验收：断网创建→移动→完成→重启，当前数据与关键事件一致；未加入进度字段或后台聚合。

## M3 — 实体关联与独立状态

- [ ] 按D06最终拓扑建立关系表、外键、去重与必要基数约束。
- [ ] 实现同事务自关联 / 防环，前端只做即时提示。
- [ ] 实现“关联到…”搜索、候选周期说明、保存与取消。
- [ ] 实现“拆解下一步”，新条目 / 位置 / 关系 / 初始事件原子创建。
- [ ] 实现真实ID标签、改名更新、+N折叠、tooltip与上级 / 下级详情。
- [ ] 实现解除、跨周期定位和归档提示，不改变条目主位置与自身状态。
- [ ] 实现选中高亮；其后实现可见端点SVG，滚动 / 缩放 / 抽屉下保持准确。
- [ ] 历史卡片不得接入当前连线；历史详情明确展示当前关联而非完整过去关系图。
- [ ] 实现删除影响预览、保留其他条目和恢复关系冲突处理。
- [ ] 验证完成 / 取消 / 重开不联动任何父子项；通过B01–B12。

验收：四个不同时间尺度条目可追溯，移动不丢关联，完成只影响自身。

## M4A — 历史与往期未完成

- [ ] 实现只读历史查询：按明确period成员去重，不把全部日任务隐式当成月计划。
- [ ] 实现statusAtEnd与laterOutcome分开，正确处理完成 / 重开 / 取消 / 移出 / 归档 / 删除。
- [ ] 实现日 / 周 / 月 / 三个月列独立翻页、空周期、历史标识、返回当前，禁止未来排期。
- [ ] 历史行禁用直接新建 / 勾选 / 拖拽，点击只打开标明“当前任务”的详情。
- [ ] 首次启动回到当前；会话中历史列保持视图，当前列跨日期自然更新。
- [ ] 显示当前内容标签与不完整历史baseline，不能用当前状态反推过去。
- [ ] 实现当前列顶部“往期未完成 · N”及原计划日期，按唯一placement派生；不按策略隐藏自动失败 / 撤销抑制 / 生效前积压 / 恢复暂停的任务。
- [ ] 实现该分组的批量安排到当前 / Later，以及单项移列 / 完成 / 取消。
- [ ] 旧周期直接完成在实际完成日可查，但不制造新的当前周期主计划。
- [ ] 通过F01–F17，包含自动策略下往期任务仍可发现，以及9月计划 / 10月完成和未运行中间周期的例子。

## M4B — 设置、自动顺延与撤销

- [ ] 实现默认day=auto、week/month=manual、cycle固定manual，Later无策略。
- [ ] 实现日 / 周 / 月设置及生效周期；启用不追溯搬动旧积压，关闭不倒退已发生安排。
- [ ] 实现统一reconcile入口：启动、唤醒、前台恢复、日历边界，不依赖退出后的常驻服务。
- [ ] 在事务内重新筛选有效旧todo，排除完成 / 取消 / 归档 / 删除及本周期撤销抑制。
- [ ] 按唯一主位置策略处理，不沿目标关系级联，截止日期保持不变。
- [ ] 原子更新placement + 每项事件 + 批次结果；追加排序不扰乱已有当前计划。
- [ ] 实现成功提示、查看批次、任务原计划来源与实际处理时间，不伪造中间月份操作。
- [ ] 实现批次撤销预览、版本复核、部分冲突跳过与反向事件，不能覆盖后续编辑。
- [ ] 持久化本周期auto_hold，撤销后重启不再次搬回；新周期和手动移动按规格处理。
- [ ] 验证重复触发、响应丢失、并发手动完成、事务中断、午夜边界与时钟异常。
- [ ] 通过C01–C08、G01–G20；不实现自动归档 / 删除或进度联动。

## M4C — 导出、迁移与恢复

- [ ] JSON导出包括设置、策略、实体、周期、位置、关系、事件、批次与撤销抑制。
- [ ] 实现大小 / schema / 日期 / 外键 / 事件链 / 关系图校验，拒绝不一致恢复数据。
- [ ] 按ARCHITECTURE §4.4校验业务字段链，允许未记录编辑的版本跳号和排序差异；不得以整份before / after全等代替，撤销版本校验仍严格。
- [ ] 恢复需预览和确认；恢复前与迁移前生成SQLite一致性备份。
- [ ] 实现原子恢复、迁移失败回滚和明确错误；验证WAL场景。
- [ ] 恢复后暂停自动补处理，用户确认才继续；重启不能绕过确认。
- [ ] 旧数据无历史时建真实baseline，保留可信导入历史ID / 时序，不伪造过去。
- [ ] 注入磁盘满、异常退出、非法导入和重复请求，验证原库和历史完整。
- [ ] 通过D04–D13，覆盖创建、改标题 / 说明、列内排序、完成后的合法导出恢复；记录真正执行结果。

M4总验收：历史不改现在、过去不会被后续状态覆盖；未完成可发现，自动顺延可追踪 / 安全撤销，恢复包含关键历史。

## M5 — Electron双平台内测

- [ ] 所有承诺OS / CPU在干净环境安装并完整执行核心流程，不以Mac代替Windows。
- [ ] 验证原生SQLite包、生产preload、输入法、剪贴板、中文 / 空格路径与文件权限。
- [ ] 验证缩放、多显示器、深色模式、关闭 / 退出 / Dock重开和睡眠唤醒。
- [ ] 真实跨日并通过可控测试时钟覆盖跨周 / 月 / 三个月；不得在生产保留时钟控制口。
- [ ] 用1,000活跃 / 10,000总条目及相应事件量测看板、历史、顺延和恢复，不虚构性能。
- [ ] 验证升级后数据 / 事件 / 策略 / 应用标识连续，明确卸载数据保留策略。
- [ ] 检查无调试端口、开发服务、任意IPC、私人repo token或任务正文遥测。
- [ ] 执行A–H全部适用场景，明确未执行项；修复发布阻断问题。
- [ ] 生成私人内测安装包、测试报告和已知限制，而不是只交付网页预览。

## R1 — 正式对外分发，另行批准

- [ ] 选定稳定bundle/app标识、用户数据目录与支持系统列表。
- [ ] 准备签名 / 公证策略并验证真实下载后的安装体验。
- [ ] 确认公开下载渠道；开发repo仍Private，客户端不携带仓库凭据。
- [ ] 说明本地数据 / 历史 / 导出未加应用层加密及用户备份责任。
- [ ] 获得明确公开发布授权后再分发，不把本次文档请求当发布授权。

## P1 / 后续，不提前实现

- [ ] 目标百分比、任务汇总、数值KR；另定口径，不与历史完成状态混淆。
- [ ] 任意未来周期排期、独立编辑三个月结束日 / 任意时长（须先批准）。
- [ ] 完整周期复盘、冻结快照、标题 / 说明 / 关系全版本恢复、Markdown导出。
- [ ] 重复任务、提醒、全局录入、普通标签及通用批量管理；往期分组批量安排已是P0。
- [ ] 连续顺延次数展示 / 限制、三个月自动顺延、自动归档策略（须单独批准）。
- [ ] 自动更新、托盘 / 菜单栏、毛玻璃视觉增强。
- [ ] 独立立项的账号 / 云同步 / Web / 移动端 / 团队 / AI / 计费。

每个实现PR说明对应需求、实际测试、实测平台、迁移和回退。文档检查只证明文件本身，不替代任何应用验收。
